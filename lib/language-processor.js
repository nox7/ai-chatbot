class LanguageProcessor{

	/**
	* Processes input
	*
	* @param {string} input
	* @param {function} onProcessFinished Callback for when processing is done
	* @return ?
	*/
	static process(input, onProcessFinished){

	}
}

module.exports = LanguageProcessor;
